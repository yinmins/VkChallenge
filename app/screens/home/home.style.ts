import { StyleSheet } from "react-native";

export const homeStyles = StyleSheet.create({
    iconContainer: {
        justifyContent: 'center',
        alignItems: 'center',
        textAlign: 'center', 
      },
    text_percent: {
        color: "#469C05",
        fontWeight: "600",
    }, 
});

