 export class AirPollution {
    private type: string;
    private percentage: string;
    private color: string;
    constructor(type: string, percentage: string, color: string){
         this.type=type;
         this.percentage=percentage;
         this.color=color;    
    }
}